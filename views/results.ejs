<h1>Results</h1>
<ul>
<% data["Search"].forEach((movie) => { %>
    <h2><%= movie["Title"] %> - <%= movie["Year"] %></h2>
    <% if(movie['Poster']!=='N/A') { %>
        <img src="<%= movie['Poster'] %>">
    <% } %>
    <hr>
<% }) %>
</ul>